import SeccionPlaceholder from '../components/SeccionPlaceholder.jsx'

export default function AuditoriaPage() {
  return (
    <SeccionPlaceholder
      titulo="Auditoría"
      descripcion="Consulta de registros de auditoría (solo lectura)."
      icono="history"
    />
  )
}
